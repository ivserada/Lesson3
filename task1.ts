const age: number = 23;
const days: number = age * 365;
const hours: number = days * 24;
const minutes: number = hours * 60;
const seconds: number = minutes * 60;
//добавить минуты

console.log(`Мне ${age * 365} дней ${hours} часов ${minutes} минут и ${seconds} секунд`);
